// --- INITIALIZATION ---
// Runs after all other scripts have loaded and defined their functions.
document.getElementById('header-name-input').value = characterData.name;
renderNavigation();
window.renderContent();
lucide.createIcons();
